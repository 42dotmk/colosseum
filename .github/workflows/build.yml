name: Colosseum CI pipeline

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - 'development'
      - 'main'

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  find_modified_files:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check modified files
        run: |
          git diff --name-only ${{ github.base_ref }} ${{ github.head_ref }} | grep -q "^senatus/"
          git diff --name-only ${{ github.base_ref }} ${{ github.head_ref }} | grep -q "^client/"
          git diff --name-only ${{ github.base_ref }} ${{ github.head_ref }} | grep -q "^judge/"

  build_senatus:
    needs: find_modified_files
    uses: ./.github/workflows/build-push-image.yml
    with:
      docker-context: ./senatus
      docker-file-path: ./senatus/Dockerfile
      docker-tags: ghcr.io/42dotmk/colosseum-senatus:latest

  build_client:
    needs: find_modified_files
    uses: ./.github/workflows/build-push-image.yml
    with:
      docker-context: ./client
      docker-file-path: ./client/Dockerfile
      docker-tags: ghcr.io/42dotmk/colosseum-client-prod:latest
